# PWA-laag Implementatieplan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Stash installeerbaar maken op het startscherm, met een schil die zonder netwerk opent en een update-flow die de gebruiker ziet.

**Architecture:** Een eigen service worker (`injectManifest`) precacht de gehashte build-assets, de icons en drie geprerenderde offline-pagina's. Navigatie gaat netwerk-eerst en het antwoord wordt nooit bewaard — pagina's verschillen hier per gebruiker én per taal. Valt het netwerk weg, dan komt de offline-pagina die bij de taalprefix van het gevraagde pad hoort. Het manifest wordt per taal geserveerd door een Nitro-route, zodat `start_url` naar de voorraad in de eigen taal wijst.

**Tech Stack:** Nuxt 4, `@vite-pwa/nuxt` ^1.1.1, `@vite-pwa/assets-generator` ^1.0.0, Workbox (via de module), `@nuxtjs/i18n` v10, Playwright, Vitest, Cloudflare Workers.

**Spec:** [docs/superpowers/specs/2026-09-25-pwa-design.md](../specs/2026-09-25-pwa-design.md)

## Global Constraints

- **Taal van de code:** alle commentaar, testnamen en commitberichten in het Nederlands, zoals de rest van deze codebase. Commentaar legt uit *waarom*, niet *wat*.
- **Commits:** conventional-commit-prefix, Nederlandse tekst, en eindigen op de `Co-Authored-By:`-regel die de eigen omgeving van de uitvoerder voorschrijft. Die regel noemt het model dat de code werkelijk schreef; uniformiteit over de branch is hier minder waard dan juistheid.
- **Routepaden komen altijd uit `routes.config.ts`.** Nooit een pad met de hand typen, in code noch in tests. Dat bestand is de enige bron voor de i18n-config, de `exclude`-lijst van `@nuxtjs/supabase` en de tests.
- **`prefix_except_default`:** alleen het Engels heeft een kaal pad. Nederlands is `/nl/...`, Frans is `/fr/...`. Een manifest met `/voorraad` erin opent een 404.
- **HTML wordt nooit gecachet.** Geen navigatie-antwoord belandt in een cache.
- **`/api/*` en Supabase-verzoeken worden niet onderschept.**
- **Kleuren:** `theme_color` `#18181b`, `background_color` `#ffffff`.
- **De gebouwde preview draait met `npx wrangler --cwd .output dev --port 3001`.** Niet `nuxt preview`; Nitro drukt deze opdracht zelf af na de build. Poort 3001 staat los van de dev-server op 3000.
- **Elke test moet aantoonbaar kunnen falen.** Waar een taak een falsificatiestap heeft, wordt die uitgevoerd en het resultaat gerapporteerd — niet aangenomen.
- **Na elke taak:** `npm run lint` en `npm run typecheck` zijn schoon voor je commit.

---

### Task 1: De module, een service worker die alleen precacht, en het testproject dat hem ziet

Deze taak legt de infrastructuur waarop de rest leunt: de module, een minimale service worker, en een tweede Playwright-configuratie die tegen de **gebouwde** app draait. Dat laatste is geen luxe. In dev is de precache leeg, dus een offline-test tegen `nuxt dev` zou een andere service worker testen dan de gebruiker krijgt.

De app is na deze taak nog niet installeerbaar: het manifest komt in Taak 5. Dat is bewust — het manifest verwijst naar icons, en die bestaan pas na Taak 2.

**Files:**
- Modify: `package.json` (dependencies en scripts)
- Create: `app/sw.ts`
- Create: `load-env.ts`
- Modify: `nuxt.config.ts`
- Modify: `playwright.config.ts`
- Create: `playwright.pwa.config.ts`
- Create: `e2e/pwa/register.spec.ts`
- Modify: `.github/workflows/ci.yml`

**Interfaces:**
- Consumes: niets uit eerdere taken.
- Produces: `app/sw.ts` (Taak 4 breidt hem uit), het npm-script `test:e2e:pwa`, en de testmap `e2e/pwa/` waar Taak 2, 4, 5 en 6 hun specs in zetten.

- [ ] **Step 1: Installeer de module**

```bash
npm install --save-dev @vite-pwa/nuxt@^1.1.1 workbox-precaching workbox-routing
```

`app/sw.ts` importeert rechtstreeks uit `workbox-precaching` en `workbox-routing`. Die horen daarom expliciet in `package.json` te staan en niet als toevallige transitieve kopie van de module: een minor upgrade daarvan zou ze anders onder je vandaan kunnen halen, en dan faalt de build op een import die je nergens hebt gedeclareerd.

Draait `npm install` een script dat om toestemming vraagt (`allowScripts` in `package.json`), meld dat dan en wacht op een beslissing — die lijst is een bewuste keuze en wordt niet stilletjes uitgebreid.

- [ ] **Step 2: Trek het laden van .env uit playwright.config.ts**

Beide Playwright-configuraties hebben dit nodig. Maak `load-env.ts` in de projectwortel met de blok-commentaar die nu in `playwright.config.ts` staat:

```ts
// De testrunner laadt .env niet vanzelf (zelfde reden als in
// test/db/helpers.ts). Een ontbrekend bestand negeren we; de tests falen dan
// alsnog met een duidelijke melding over de ontbrekende variabele.
//
// Dit staat apart omdat zowel playwright.config.ts als
// playwright.pwa.config.ts het nodig heeft, en een tweede kopie op termijn
// uit elkaar loopt met de eerste.
export function loadEnv(): void {
  try {
    process.loadEnvFile()
  } catch (cause) {
    // ENOENT betekent: er is geen .env. Dat is normaal in CI, waar de
    // variabelen rechtstreeks in de omgeving staan. Elke andere fout betekent
    // dat er wél een bestand is maar dat het niet te lezen valt — dat stil
    // inslikken kost later uren zoeken naar een variabele die er wel lijkt te
    // staan.
    if ((cause as NodeJS.ErrnoException).code !== 'ENOENT') {
      throw new Error('.env bestaat maar is niet te lezen', { cause })
    }
  }
}
```

Vervang het inline-blok in `playwright.config.ts` door `import { loadEnv } from './load-env'` en een aanroep `loadEnv()`. Voeg daar ook `testIgnore: '**/pwa/**'` toe aan de config, zodat de gewone e2e-run de PWA-specs niet oppikt:

```ts
export default defineConfig({
  testDir: './e2e',
  // De PWA-specs hebben een gebouwde app nodig en draaien via
  // playwright.pwa.config.ts op poort 3001. Zonder deze uitsluiting zou
  // `npm run test:e2e` ze tegen de dev-server draaien, waar de precache leeg
  // is — en dan staan ze groen zonder iets te bewijzen.
  testIgnore: '**/pwa/**',
  use: { baseURL: 'http://localhost:3000' },
  webServer: { /* ongewijzigd */ },
})
```

- [ ] **Step 3: Schrijf de faalende test**

Maak `e2e/pwa/register.spec.ts`:

```ts
import { test, expect } from '@playwright/test'

// Service workers werken alleen in een secure context. localhost telt als
// secure, dus dit werkt zonder https.
test('de service worker registreert zich in de gebouwde app', async ({ page }) => {
  await page.goto('/')

  const scriptURL = await page.evaluate(async () => {
    const registratie = await navigator.serviceWorker.ready
    return registratie.active?.scriptURL ?? null
  })

  expect(scriptURL).toContain('/sw.js')
})
```

- [ ] **Step 4: Maak playwright.pwa.config.ts**

```ts
import { defineConfig } from '@playwright/test'
import { loadEnv } from './load-env'

loadEnv()

export default defineConfig({
  testDir: './e2e/pwa',
  use: { baseURL: 'http://localhost:3001' },
  webServer: {
    // De build hoort bij het starten: een preview tegen een oude .output is
    // precies de val waar dit project eerder in liep (zie "Valkuilen bij
    // lokaal ontwikkelen" in open-bevindingen.md). Daarom ook
    // reuseExistingServer: false — een hergebruikte server serveert stilletjes
    // de vorige build.
    //
    // `npx wrangler --cwd .output dev` en niet `nuxt preview`: Nitro drukt
    // deze opdracht zelf af aan het einde van de build, en dit is dezelfde
    // runtime als productie.
    command: 'npm run build && npx wrangler --cwd .output dev --port 3001',
    url: 'http://localhost:3001/api/health',
    reuseExistingServer: false,
    timeout: 300_000,
  },
})
```

- [ ] **Step 5: Voeg het script toe aan package.json**

```json
"test:e2e:pwa": "playwright test --config playwright.pwa.config.ts"
```

- [ ] **Step 6: Draai de test en controleer dat hij faalt**

Run: `npm run test:e2e:pwa`
Expected: FAIL — `navigator.serviceWorker.ready` lost nooit op, dus de test loopt in een timeout. Er is nog geen service worker.

- [ ] **Step 7: Maak de minimale service worker**

Maak `app/sw.ts`:

```ts
/// <reference lib="webworker" />
import { cleanupOutdatedCaches, precacheAndRoute } from 'workbox-precaching'

declare const self: ServiceWorkerGlobalScope

// cleanupOutdatedCaches ruimt de precache van vorige builds op. Zonder dit
// stapelen oude versies zich op in de opslag van het toestel, en op iOS —
// waar de quota krap zijn — is dat de manier om de hele cache te laten
// wegvallen.
cleanupOutdatedCaches()

// self.__WB_MANIFEST wordt door de build gevuld met de gehashte assets.
// De navigatie-afhandeling komt in Taak 4; deze worker precacht voorlopig
// alleen.
precacheAndRoute(self.__WB_MANIFEST)
```

- [ ] **Step 8: Configureer de module in nuxt.config.ts**

Voeg toe aan `modules`: `'@vite-pwa/nuxt'`. Voeg daarna dit blok toe:

```ts
  pwa: {
    strategies: 'injectManifest',
    // Niet 'app'. Deze module zoekt `${srcDir}/${filename}` op ten opzichte
    // van Vite's eigen root, en Nuxt 4 zet die root al op nuxt.options.srcDir
    // — dat is hier `app/`. 'app' er nog eens bovenop laat de build zoeken
    // naar app/app/sw.ts. Zie vite-plugin-pwa's resolveSwPaths().
    srcDir: '.',
    filename: 'sw.ts',
    // 'prompt' en niet 'autoUpdate': de gebruiker bevestigt zelf. Elke merge
    // naar main deployt, dus updates komen vaak; automatisch herladen zou
    // dat onder een half ingevuld formulier vandaan doen.
    registerType: 'prompt',
    // Het manifest komt uit een eigen Nitro-route, één per taal (Taak 5).
    // De module mag er zelf geen genereren, anders staan er twee
    // <link rel="manifest"> in de head en wint de verkeerde.
    manifest: false,
    injectManifest: {
      globPatterns: ['**/*.{js,css,html,svg,png,ico,woff2}'],
    },
    devOptions: {
      // De standaard navigateFallbackAllowlist van deze module is /\//, wat
      // elk pad met een schuine streep matcht (vite-pwa/nuxt#139). Voor een
      // SSR-app onderschept dat alles. De PWA-tests draaien sowieso tegen de
      // gebouwde app, dus in dev hoeft de worker helemaal niet mee te doen.
      enabled: false,
    },
  },
```

En in het bestaande `nitro`-blok een route-regel voor de worker zelf:

```ts
    routeRules: {
      // sw.js is niet gehasht: elke build levert dezelfde bestandsnaam. Zonder
      // korte cache-header blijft een oude worker uren in omloop, en dan
      // draait de gebruiker een oude schil terwijl /api/health keurig de
      // nieuwe SHA meldt.
      '/sw.js': { headers: { 'cache-control': 'public, max-age=0, must-revalidate' } },
    },
```

Let op: `routeRules` hoort op het niveau van `nuxt.config.ts` zelf, niet binnen `nitro`. Zet hem naast `nitro`, niet erin.

- [ ] **Step 9: Draai de test en controleer dat hij slaagt**

Run: `npm run test:e2e:pwa`
Expected: PASS

- [ ] **Step 10: Controleer de cache-header in het bouwresultaat**

Run: `grep -A1 'sw.js' .output/public/_headers`
Expected: de regel `cache-control: public, max-age=0, must-revalidate` staat eronder.

Staat hij er niet, dan schrijft Nitro deze route-regel niet naar `_headers` voor de Cloudflare-preset. Zet de header in dat geval rechtstreeks in `public/_headers` (Nitro voegt die samen met de gegenereerde), en noteer in het commentaar waarom.

- [ ] **Step 11: Voeg het PWA-testproject toe aan CI**

In `.github/workflows/ci.yml`, in de `e2e`-job, tussen "End-to-endtests" en "Rapport bewaren bij een mislukking":

```yaml
      # Deze draaien tegen een gebouwde app op poort 3001. De build erbij
      # kost tijd, maar in dev is de precache leeg en bewijst een offline-test
      # niets over wat de gebruiker krijgt.
      - name: PWA-tests tegen de gebouwde app
        run: npm run test:e2e:pwa
```

Geen `env:`-blok nodig: de stap "Supabase-gegevens in de omgeving zetten" eerder in deze job schrijft `SUPABASE_URL` en `SUPABASE_KEY` naar `$GITHUB_ENV`, en die erft elke volgende stap.

Zet de stap vóór "Rapport bewaren bij een mislukking", zodat die `if: failure()`-stap ook het rapport van deze run meeneemt.

- [ ] **Step 12: Ruim achtergebleven processen op**

`wrangler dev` start een `workerd`-kindproces dat een `taskkill` op de ouder overleeft en poort 3001 bezet houdt. Controleer na de testrun:

```bash
npx --yes kill-port 3001 || true
```

Werkt dat niet op Windows, gebruik dan PowerShell: zoek de luisteraar op poort 3001 en stop eerst het `node`-proces met `wrangler` in zijn commandline, daarna het `workerd`-proces. Noteer dit in Taak 8 bij de open bevindingen.

- [ ] **Step 13: Commit**

```bash
git add package.json package-lock.json app/sw.ts load-env.ts nuxt.config.ts playwright.config.ts playwright.pwa.config.ts e2e/pwa/register.spec.ts .github/workflows/ci.yml
git commit -m "feat: service worker die de build-assets precacht

Plus een tweede Playwright-configuratie die tegen de gebouwde app draait.
In dev is de precache leeg; een offline-test daar zou een andere service
worker testen dan de gebruiker krijgt.

Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>"
```

---

### Task 2: Het icoon

De app draagt nu nog het standaard Nuxt-logo. In een browsertabblad valt dat niemand op; op een startscherm wel.

**Files:**
- Create: `public/icon.svg`
- Create: `pwa-assets.config.ts`
- Modify: `package.json` (devDependency en script)
- Replace: `public/favicon.ico` (gegenereerd)
- Create (gegenereerd): `public/pwa-64x64.png`, `public/pwa-192x192.png`, `public/pwa-512x512.png`, `public/maskable-icon-512x512.png`, `public/apple-touch-icon-180x180.png` — zes bestanden met de favicon mee, niet zeven: `minimal2023Preset` levert geen `favicon.svg`. Die rol vervult de bron-SVG zelf.
- Create: `e2e/pwa/icons.spec.ts`

**Interfaces:**
- Consumes: de testmap `e2e/pwa/` en `npm run test:e2e:pwa` uit Taak 1.
- Produces: de zeven bestandsnamen hierboven. Taak 5 verwijst er vanuit het manifest naar met exact deze namen.

- [ ] **Step 1: Installeer de generator**

```bash
npm install --save-dev @vite-pwa/assets-generator@^1.0.0
```

- [ ] **Step 2: Maak het bronbestand**

Maak `public/icon.svg`. Het teken blijft binnen de centrale 80% van het vlak, want de maskable-variant wordt door Android bijgesneden:

```svg
<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 512 512" width="512" height="512">
  <rect width="512" height="512" fill="#18181b"/>
  <rect x="150" y="131" width="212" height="44" rx="16" fill="#fafaf9"/>
  <rect x="168" y="181" width="176" height="200" rx="34" fill="#fafaf9"/>
  <circle cx="218" cy="301" r="23.5" fill="#18181b"/>
  <circle cx="294" cy="301" r="23.5" fill="#18181b"/>
  <circle cx="256" cy="348" r="23.5" fill="#18181b"/>
</svg>
```

- [ ] **Step 3: Maak pwa-assets.config.ts**

```ts
import { defineConfig, minimal2023Preset } from '@vite-pwa/assets-generator/config'

// Eén bron, alle formaten afgeleid. Het icoon later vervangen is daarmee één
// bestand omwisselen en dit script opnieuw draaien, in plaats van zeven
// bestanden met de hand opnieuw exporteren.
export default defineConfig({
  headLinkOptions: { preset: '2023' },
  preset: minimal2023Preset,
  images: ['public/icon.svg'],
})
```

Klopt de importnaam niet, kijk dan in `node_modules/@vite-pwa/assets-generator/dist/config.d.ts` welke presets geëxporteerd worden en gebruik die naam.

- [ ] **Step 4: Voeg het script toe aan package.json**

```json
"icons": "pwa-assets-generator"
```

- [ ] **Step 5: Genereer en controleer de bestandsnamen**

Run: `npm run icons && ls public/`
Expected: `favicon.ico`, `pwa-64x64.png`, `pwa-192x192.png`, `pwa-512x512.png`, `maskable-icon-512x512.png`, `apple-touch-icon-180x180.png`.

**Bekijk de gegenereerde PNG's ook werkelijk**, en niet alleen hun namen. De maskable- en apple-varianten krijgen van de preset standaard een *witte* achtergrond, waardoor de bijna-zwarte tegel als klein blokje op een wit vlak komt te staan. Android snijdt een maskable icon tot een cirkel en iOS legt er zijn eigen afronding op, dus dat levert een witte rand rond het teken op in plaats van een gevulde tegel. Overschrijf daarom in `pwa-assets.config.ts` de achtergrond voor beide:

```ts
export default defineConfig({
  headLinkOptions: { preset: '2023' },
  preset: {
    ...minimal2023Preset,
    // De preset zet standaard wit achter deze twee. Dat is precies verkeerd
    // voor een teken dat zijn eigen achtergrond meebrengt: het masker van
    // Android en iOS snijdt dan een witte rand rond een zwarte tegel.
    maskable: {
      ...minimal2023Preset.maskable,
      resizeOptions: { ...minimal2023Preset.maskable.resizeOptions, background: '#18181b' },
    },
    apple: {
      ...minimal2023Preset.apple,
      resizeOptions: { ...minimal2023Preset.apple.resizeOptions, background: '#18181b' },
    },
  },
  images: ['public/icon.svg'],
})
```

Klopt de vorm van `minimal2023Preset` niet met wat hierboven staat, kijk dan in `node_modules/@vite-pwa/assets-generator/dist/config.d.ts` naar de werkelijke velden en pas het aan.

Wijken de namen af, gebruik dan de werkelijke namen in Step 6 en in Taak 5 — en noteer ze in het commentaar van `pwa-assets.config.ts`, zodat de volgende lezer ze niet hoeft te raden.

- [ ] **Step 6: Schrijf de test**

Maak `e2e/pwa/icons.spec.ts`:

```ts
import { test, expect } from '@playwright/test'

// De icons zijn statische bestanden, maar ze horen ook werkelijk in de
// gebouwde output te belanden. Een manifest dat naar een 404 wijst geeft
// geen foutmelding — de installatie toont dan gewoon een leeg vierkant.
const icons = [
  '/pwa-64x64.png',
  '/pwa-192x192.png',
  '/pwa-512x512.png',
  '/maskable-icon-512x512.png',
  '/apple-touch-icon-180x180.png',
]

test('elk icoon wordt geserveerd als afbeelding', async ({ request }) => {
  for (const pad of icons) {
    const antwoord = await request.get(pad)
    expect(antwoord.status(), `status voor ${pad}`).toBe(200)
    expect(antwoord.headers()['content-type'], `content-type voor ${pad}`).toContain('image/')
  }
})

test('de favicon is niet meer die van Nuxt', async ({ request }) => {
  const antwoord = await request.get('/favicon.ico')
  expect(antwoord.status()).toBe(200)
  // Het Nuxt-logo is 4286 bytes. De gegenereerde favicon is een 48x48 uit
  // onze eigen SVG en heeft daarmee een andere grootte. Dit is een grove
  // controle, maar hij vangt wel het geval waarin de generator niet gedraaid
  // is en het oude bestand blijft staan.
  const body = await antwoord.body()
  expect(body.byteLength).not.toBe(4286)
})
```

- [ ] **Step 7: Draai de test**

Run: `npm run test:e2e:pwa`
Expected: PASS (beide nieuwe tests plus de registratietest uit Taak 1)

- [ ] **Step 8: Falsificatie — controleer dat de test iets bewijst**

Hernoem `public/pwa-192x192.png` tijdelijk naar `public/pwa-192x192.png.bak` en draai `npm run test:e2e:pwa` opnieuw.
Expected: FAIL op `status voor /pwa-192x192.png`.

Zet het bestand daarna terug en bevestig dat de test weer slaagt. Rapporteer wat je zag.

- [ ] **Step 9: Commit**

```bash
git add public/ pwa-assets.config.ts package.json package-lock.json e2e/pwa/icons.spec.ts
git commit -m "feat: een eigen icoon, afgeleid uit één bron-SVG

De app droeg tot nu toe het logo van Nuxt. Op een startscherm is dat het
eerste wat opvalt.

Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>"
```

---

### Task 3: De offline-route en de afleiding van pad naar taal

De service worker staat buiten Vue en heeft dus geen `useI18n()`. De taal moet uit het pad komen. Dat kan, want `prefix_except_default` zet de taal vooraan.

**Files:**
- Modify: `routes.config.ts`
- Create: `app/pages/offline.vue`
- Modify: `i18n/locales/en.json`, `i18n/locales/nl.json`, `i18n/locales/fr.json`
- Modify: `nuxt.config.ts` (prerender)
- Create: `test/routes/offline-path.test.ts`

**Interfaces:**
- Consumes: niets uit eerdere taken.
- Produces: `offlinePathFor(pathname: string): string` uit `routes.config.ts`, plus de routesleutel `'offline'`. Taak 4 importeert die functie in de service worker.

- [ ] **Step 1: Schrijf de faalende test**

Maak `test/routes/offline-path.test.ts`:

```ts
import { describe, it, expect } from 'vitest'
import { offlinePathFor } from '../../routes.config'

// De service worker draait buiten Vue en kan useI18n() niet gebruiken. De
// taal moet dus uit het pad komen. Dit is de hele afleiding, en daarmee het
// stuk dat een echte unittest kan krijgen in plaats van een browsertest die
// hoopt.
describe('offlinePathFor', () => {
  it('geeft de Engelse pagina voor een pad zonder prefix', () => {
    expect(offlinePathFor('/inventory')).toBe('/offline')
  })

  it('geeft de Nederlandse pagina voor een /nl-pad', () => {
    expect(offlinePathFor('/nl/voorraad')).toBe('/nl/offline')
  })

  it('geeft de Franse pagina voor een /fr-pad', () => {
    expect(offlinePathFor('/fr/stock')).toBe('/fr/hors-ligne')
  })

  it('geeft de Engelse pagina voor de wortel', () => {
    expect(offlinePathFor('/')).toBe('/offline')
  })

  // 'en' is de standaardtaal en heeft dus geen prefix: /en/... bestaat niet
  // als route. Zou deze functie 'en' tóch als prefix herkennen, dan bouwt ze
  // een pad dat nergens heen gaat.
  it('behandelt /en niet als taalprefix', () => {
    expect(offlinePathFor('/en/inventory')).toBe('/offline')
  })

  // Een taalcode dieper in het pad is een gewone map, geen prefix.
  it('laat zich niet misleiden door een map die op een taalcode lijkt', () => {
    expect(offlinePathFor('/settings/nl')).toBe('/offline')
  })
})
```

- [ ] **Step 2: Draai de test en controleer dat hij faalt**

Run: `npm test -- offline-path`
Expected: FAIL — `offlinePathFor` bestaat niet.

- [ ] **Step 3: Breid routes.config.ts uit**

Voeg de route toe aan `routePaths`, na `'confirm'`:

```ts
  'offline': { en: '/offline', nl: '/offline', fr: '/hors-ligne' },
```

Voeg `'offline'` toe aan `publicRoutes`:

```ts
export const publicRoutes = ['login', 'confirm', 'invite/[token]', 'offline'] as const
```

En voeg onderaan, na `routeGlob`, de functie toe:

```ts
/**
 * Welke offline-pagina hoort bij een pad?
 *
 * De service worker gebruikt dit als het netwerk wegvalt. Hij draait buiten
 * Vue en heeft dus geen useI18n(); de taal moet uit het pad komen. Dat kan,
 * want `prefix_except_default` zet de taal vooraan.
 *
 * De standaardtaal heeft geen prefix en is daarom de uitkomst zodra er geen
 * bekende prefix staat — ook voor een pad dat met /en begint, want die route
 * bestaat niet.
 */
export function offlinePathFor(pathname: string): string {
  const eerste = pathname.split('/')[1]
  const locale = localeCodes.find((code) => code !== defaultLocale && code === eerste)
  return routePath('offline', locale ?? defaultLocale)
}
```

- [ ] **Step 4: Draai de test en controleer dat hij slaagt**

Run: `npm test -- offline-path`
Expected: PASS, zes tests.

- [ ] **Step 5: Voeg de vertalingen toe**

In `i18n/locales/en.json`, na het `landing`-blok:

```json
  "offline": {
    "title": "You are offline",
    "body": "Stash needs a connection for this page. Nothing is lost — try again once you are back online.",
    "retry": "Try again"
  },
```

In `nl.json`:

```json
  "offline": {
    "title": "Je bent offline",
    "body": "Stash heeft verbinding nodig voor deze pagina. Er gaat niets verloren — probeer opnieuw zodra je weer online bent.",
    "retry": "Opnieuw proberen"
  },
```

In `fr.json`:

```json
  "offline": {
    "title": "Vous êtes hors ligne",
    "body": "Stash a besoin d'une connexion pour cette page. Rien n'est perdu — réessayez dès que vous êtes de nouveau en ligne.",
    "retry": "Réessayer"
  },
```

- [ ] **Step 6: Maak de offline-pagina**

Maak `app/pages/offline.vue`:

```vue
<script setup lang="ts">
// Geen data, geen auth, geen netwerk. Deze pagina moet te tonen zijn als er
// niets meer werkt, dus alles wat ze nodig heeft zit in de geprerenderde
// HTML en in de geprecachte assets.
const { t } = useI18n()

function opnieuw() {
  window.location.reload()
}
</script>

<template>
  <UContainer class="py-16 text-center">
    <h1 class="text-3xl font-bold">{{ t('offline.title') }}</h1>
    <p class="mt-3 text-lg text-muted">{{ t('offline.body') }}</p>
    <UButton class="mt-8" size="lg" @click="opnieuw">
      {{ t('offline.retry') }}
    </UButton>
  </UContainer>
</template>
```

- [ ] **Step 7: Laat de drie offline-pagina's prerenderen**

In `nuxt.config.ts`, binnen het `nitro`-blok:

```ts
    prerender: {
      // Statische bestanden in .output/public, zodat de service worker ze kan
      // precachen en Cloudflare ze rechtstreeks serveert. Ze staan in
      // publicRoutes, anders stuurt de auth-guard ze tijdens het prerenderen
      // naar de inlogpagina.
      routes: localeCodes.map((code) => routePath('offline', code)),
    },
```

Vul de import bovenaan `nuxt.config.ts` aan zodat `localeCodes` erbij staat.

- [ ] **Step 8: Bouw en noteer de werkelijke bestandsnamen**

Run: `npm run build && find .output/public -name '*.html'`
Expected: drie bestanden. Noteer de exacte vorm — waarschijnlijk `offline/index.html`, `nl/offline/index.html` en `fr/hors-ligne/index.html`.

**Deze namen zijn van belang voor Taak 4.** Workbox zoekt in de precache op exacte sleutel; `/offline` en `/offline/index.html` zijn niet hetzelfde. Schrijf op wat je ziet.

- [ ] **Step 9: Controleer de pagina in de browser**

Run: `npm run dev`, open `http://localhost:3000/nl/offline`
Expected: de Nederlandse tekst, met een werkende knop.

- [ ] **Step 10: Commit**

```bash
git add routes.config.ts app/pages/offline.vue i18n/locales/ nuxt.config.ts test/routes/offline-path.test.ts
git commit -m "feat: een offline-pagina per taal, en de afleiding ernaartoe

De service worker draait buiten Vue en kan useI18n() niet gebruiken, dus de
taal komt uit de prefix van het gevraagde pad.

Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>"
```

---

### Task 4: De navigatie-afhandeling in de service worker

Het hart van deze ronde. Netwerk-eerst, antwoord nooit bewaren, en bij falen de offline-pagina die bij de taal hoort.

**Files:**
- Modify: `app/sw.ts`
- Create: `e2e/pwa/offline.spec.ts`

**Interfaces:**
- Consumes: `offlinePathFor()` uit Taak 3, de service worker uit Taak 1, de bestandsnamen uit Taak 3 Step 8.
- Produces: het offline-gedrag waar Taak 6 zijn negatieve test tegenaan legt.

- [ ] **Step 1: Schrijf de faalende tests**

Maak `e2e/pwa/offline.spec.ts`:

```ts
import { test, expect } from '@playwright/test'
import en from '../../i18n/locales/en.json' with { type: 'json' }
import nl from '../../i18n/locales/nl.json' with { type: 'json' }

// De landingspagina staat niet in routePaths: bij `prefix_except_default` ís
// de prefix de route, dus er valt niets te vertalen. Vandaar deze twee
// letterlijk, als enige uitzondering op "paden komen uit routes.config.ts".
//
// Het zijn bovendien publieke pagina's, dus er is geen login nodig om het
// navigatiegedrag van de service worker te toetsen.
const landing = { en: '/', nl: '/nl' } as const

// De service worker moet geïnstalleerd zijn én de precache gevuld hebben voor
// een offline-test iets zegt. navigator.serviceWorker.ready wacht op
// activatie; precacheAndRoute vult de cache tijdens install, dus na ready
// staat alles klaar.
async function wachtOpServiceWorker(page: import('@playwright/test').Page) {
  await page.evaluate(() => navigator.serviceWorker.ready)
}

test('offline krijg je de offline-pagina van je eigen taal', async ({ page, context }) => {
  await page.goto(landing.en)
  await wachtOpServiceWorker(page)

  await context.setOffline(true)

  await page.goto(landing.nl)
  await expect(page.getByText(nl.offline.title)).toBeVisible()
})

// Deze test is er omdat de vorige alleen niet genoeg is. Een afleiding die
// altijd de Engelse pagina teruggeeft haalt een test die enkel Nederlands
// controleert nooit — maar een die enkel Engels controleert wél. Beide
// richtingen in één run is de enige vorm die de fout vangt.
test('een andere taalprefix geeft een andere offline-pagina', async ({ page, context }) => {
  await page.goto(landing.en)
  await wachtOpServiceWorker(page)

  await context.setOffline(true)

  await page.goto(landing.nl)
  await expect(page.getByText(nl.offline.title)).toBeVisible()

  await page.goto(landing.en)
  await expect(page.getByText(en.offline.title)).toBeVisible()
})

test('een gewone pagina belandt niet in de cache', async ({ page }) => {
  await page.goto(landing.en)
  await wachtOpServiceWorker(page)

  const gecachet = await page.evaluate(async () => {
    const namen = await caches.keys()
    for (const naam of namen) {
      const cache = await caches.open(naam)
      for (const verzoek of await cache.keys()) {
        // De landingspagina is server-gerenderd en verschilt per gebruiker en
        // per taal. Staat ze in een cache, dan kan ze aan de verkeerde
        // persoon geserveerd worden.
        if (new URL(verzoek.url).pathname === '/') return true
      }
    }
    return false
  })

  expect(gecachet).toBe(false)
})

// De service worker mag API-verkeer met rust laten. Offline hoort /api/health
// dus gewoon te mislukken; lost de aanroep wél op, dan onderschept of cachet
// de worker iets wat hij niet mag aanraken — en dan kan een antwoord van de
// ene gebruiker bij de andere terechtkomen.
test('de service worker raakt /api niet aan', async ({ page, context }) => {
  await page.goto(landing.en)
  await wachtOpServiceWorker(page)

  await context.setOffline(true)

  const gelukt = await page.evaluate(async () => {
    try {
      await fetch('/api/health')
      return true
    } catch {
      return false
    }
  })

  expect(gelukt).toBe(false)
})
```

- [ ] **Step 2: Draai de tests en controleer dat ze falen**

Run: `npm run test:e2e:pwa`
Expected: de twee offline-tests FAIL — zonder navigatie-afhandeling geeft de browser zijn eigen foutpagina.

De cachetest en de `/api`-test slagen nu al, en dat is in orde: ze bewaken vanaf hier dat het zo blíjft. Zouden ze rood staan, dan cachet of onderschept de worker uit Taak 1 al iets dat hij niet mag — meld dat dan voor je verder gaat.

- [ ] **Step 3: Voeg de navigatie-afhandeling toe**

Breid `app/sw.ts` uit:

```ts
/// <reference lib="webworker" />
import { cleanupOutdatedCaches, matchPrecache, precacheAndRoute } from 'workbox-precaching'
import { NavigationRoute, registerRoute } from 'workbox-routing'
import { offlinePathFor } from '../routes.config'

declare const self: ServiceWorkerGlobalScope

cleanupOutdatedCaches()
precacheAndRoute(self.__WB_MANIFEST)

// Netwerk-eerst, en het antwoord wordt niet bewaard.
//
// Dat laatste is geen vergetelheid maar de kern. Een gecachte /nl/voorraad
// bevat de naam van een huishouden en straks de inhoud ervan; op een gedeeld
// toestel, of na uitloggen, is dat precies het verkeerde bestand om nog te
// hebben.
//
// Dit is ook de reden dat hier geen workbox.navigateFallback staat. Die bouwt
// een route die de fallback áltijd uit de precache serveert, niet alleen bij
// netwerkfalen — wat voor een SSR-app neerkomt op server-rendering uitzetten.
registerRoute(
  new NavigationRoute(async ({ request }) => {
    try {
      return await fetch(request)
    } catch {
      const pad = offlinePathFor(new URL(request.url).pathname)
      return (await matchPrecache(pad)) ?? Response.error()
    }
  }),
)
```

Gebruik in `matchPrecache` de vorm die overeenkomt met wat je in Taak 3 Step 8 in `.output/public` zag. Prerendert Nuxt naar `offline/index.html`, dan vindt `matchPrecache('/offline')` niets en moet er `matchPrecache(\`${pad}/index.html\`)` staan. Kies er één, en zet erboven een commentaarregel die de waargenomen indeling noemt — zodat de volgende lezer niet opnieuw hoeft te zoeken.

- [ ] **Step 4: Draai de tests en controleer dat ze slagen**

Run: `npm run test:e2e:pwa`
Expected: PASS, alle tests.

- [ ] **Step 5: Falsificatie — haal de bescherming weg**

Zet het hele `registerRoute(...)`-blok tijdelijk in commentaar en draai `npm run test:e2e:pwa`.
Expected: de twee offline-tests worden rood; de cachetest, de icoontests en de registratietest blijven groen.

Zet het blok daarna terug. Verander vervolgens in `routes.config.ts` de functie `offlinePathFor` zo dat ze altijd `routePath('offline', defaultLocale)` teruggeeft, en draai opnieuw.
Expected: `een andere taalprefix geeft een andere offline-pagina` wordt rood, en ook de unittests uit Taak 3.

Zet alles terug en rapporteer wat je bij beide stappen zag. Bleef er iets groen dat rood had moeten worden, meld dat — dan test het niet wat de naam belooft.

- [ ] **Step 6: Commit**

```bash
git add app/sw.ts e2e/pwa/offline.spec.ts
git commit -m "feat: offline krijg je de offline-pagina van je eigen taal

Netwerk-eerst, en het antwoord wordt niet bewaard: pagina's verschillen hier
per gebruiker en per taal, dus gecachte HTML is een lek en geen optimalisatie.

Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>"
```

---

### Task 5: De drie manifesten

Hiermee wordt de app installeerbaar, en opent het startscherm-icoon de voorraad in de taal waarin je installeerde.

**Files:**
- Create: `server/routes/manifest/[locale].get.ts`
- Modify: `app/app.vue`
- Create: `e2e/pwa/manifest.spec.ts`

**Interfaces:**
- Consumes: de iconbestandsnamen uit Taak 2, `routePath()` en `localeCodes` uit `routes.config.ts`.
- Produces: `/manifest/en`, `/manifest/nl` en `/manifest/fr`.

- [ ] **Step 1: Schrijf de faalende test**

Maak `e2e/pwa/manifest.spec.ts`:

```ts
import { test, expect } from '@playwright/test'
import { localeCodes, routePath } from '../../routes.config'

// Eén manifest per taal, want start_url bepaalt wat het icoon op het
// startscherm opent — en dat is per taal een ander pad. Ze delen id, zodat
// de browser dit als één app ziet en niet als drie.
test('elk manifest opent de voorraad in zijn eigen taal', async ({ request }) => {
  for (const taal of localeCodes) {
    const antwoord = await request.get(`/manifest/${taal}`)

    expect(antwoord.status(), `status voor ${taal}`).toBe(200)
    expect(antwoord.headers()['content-type'], `content-type voor ${taal}`)
      .toContain('application/manifest+json')

    const manifest = await antwoord.json()
    expect(manifest.start_url, `start_url voor ${taal}`).toBe(routePath('inventory', taal))
    expect(manifest.id, `id voor ${taal}`).toBe('/')
    expect(manifest.scope, `scope voor ${taal}`).toBe('/')
  }
})

test('een onbekende taal geeft geen manifest', async ({ request }) => {
  const antwoord = await request.get('/manifest/de')
  expect(antwoord.status()).toBe(404)
})

test('de pagina verwijst naar het manifest van de getoonde taal', async ({ page }) => {
  await page.goto('/nl')
  const href = await page.getAttribute('link[rel="manifest"]', 'href')
  expect(href).toBe('/manifest/nl')
})
```

- [ ] **Step 2: Draai de test en controleer dat hij faalt**

Run: `npm run test:e2e:pwa`
Expected: FAIL — de route bestaat niet, dus 404 voor alle drie de talen.

- [ ] **Step 3: Maak de manifest-route**

Maak `server/routes/manifest/[locale].get.ts`:

```ts
import { localeCodes, routePath, type LocaleCode } from '../../../routes.config'
import en from '../../../i18n/locales/en.json'
import nl from '../../../i18n/locales/nl.json'
import fr from '../../../i18n/locales/fr.json'

// De naam en de tagline komen uit dezelfde vertaalbestanden als de rest van
// de app. Ze hier overtypen zou betekenen dat een aangepaste tagline op twee
// plekken bijgewerkt moet worden, en de tweede wordt vergeten.
const vertalingen = { en, nl, fr }

// Dezelfde waarden als de `language`-velden in de i18n-config van
// nuxt.config.ts. Een manifest verwacht een BCP 47-tag, geen taalcode.
const talen: Record<LocaleCode, string> = {
  en: 'en-GB',
  nl: 'nl-BE',
  fr: 'fr-BE',
}

export default defineEventHandler((event) => {
  const locale = getRouterParam(event, 'locale') as LocaleCode

  if (!localeCodes.includes(locale)) {
    throw createError({ statusCode: 404, statusMessage: 'Onbekende taal' })
  }

  const bundel = vertalingen[locale]

  setResponseHeader(event, 'content-type', 'application/manifest+json')

  return {
    // Gelijk voor alle drie: zo ziet de browser dit als één app in plaats van
    // als drie aparte installaties.
    id: '/',
    name: bundel.app.name,
    short_name: bundel.app.name,
    description: bundel.app.tagline,
    lang: talen[locale],
    // Mét taalprefix. `prefix_except_default` geeft alleen het Engels een
    // kaal pad; /voorraad zonder /nl ervoor bestaat niet en zou het icoon op
    // een 404 laten openen.
    start_url: routePath('inventory', locale),
    // De app loopt over /, /nl/* en /fr/*, dus de scope is de wortel.
    scope: '/',
    display: 'standalone',
    theme_color: '#18181b',
    // Dit is de kleur van het opstartscherm. De app opent licht, dus wit —
    // anders zie je bij elke start een zwarte flits.
    background_color: '#ffffff',
    icons: [
      { src: '/pwa-64x64.png', sizes: '64x64', type: 'image/png' },
      { src: '/pwa-192x192.png', sizes: '192x192', type: 'image/png' },
      { src: '/pwa-512x512.png', sizes: '512x512', type: 'image/png' },
      { src: '/maskable-icon-512x512.png', sizes: '512x512', type: 'image/png', purpose: 'maskable' },
    ],
  }
})
```

`orientation` staat er bewust niet in: portret afdwingen is op een tablet onbeleefd.

- [ ] **Step 4: Verwijs vanuit app.vue naar het juiste manifest**

In `app/app.vue`, in het `<script setup>`-blok:

```ts
const { locale } = useI18n()

// Eén <link rel="manifest">, maar wel naar het manifest van de taal die je nu
// ziet. Installeer je vanuit het Nederlands, dan opent het icoon straks
// /nl/voorraad en niet de Engelse pagina.
useHead({
  link: [{ rel: 'manifest', href: () => `/manifest/${locale.value}` }],
})
```

`useI18n()` wordt in dit bestand al aangeroepen voor `t`; breid die destructurering uit in plaats van een tweede aanroep toe te voegen.

- [ ] **Step 5: Draai de test en controleer dat hij slaagt**

Run: `npm run test:e2e:pwa`
Expected: PASS

- [ ] **Step 6: Falsificatie**

Verander in de manifest-route `start_url` tijdelijk in `routePath('inventory', 'en')` voor alle talen, en draai opnieuw.
Expected: `start_url voor nl` en `start_url voor fr` worden rood; `en` blijft groen.

Zet het terug en rapporteer.

- [ ] **Step 7: Controleer de installatie met de hand**

Run: `npm run build && npx wrangler --cwd .output dev --port 3001`, open `http://localhost:3001/nl` in Chrome, en kijk in DevTools bij Application → Manifest.
Expected: naam `Stash`, de Nederlandse omschrijving, `start_url` `/nl/voorraad`, en de icons worden getoond zonder fout.

Stop daarna de server en controleer dat poort 3001 vrij is (zie Taak 1 Step 12).

- [ ] **Step 8: Commit**

```bash
git add server/routes/manifest/ app/app.vue e2e/pwa/manifest.spec.ts
git commit -m "feat: een manifest per taal, zodat het icoon je eigen taal opent

Een manifest heeft één start_url. Met /inventory erin opent het startscherm-
icoon van een Nederlandse gebruiker voorgoed een Engelse pagina.

Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>"
```

---

### Task 6: De update-melding

Elke merge naar main deployt. Zonder zichtbare update blijft een gebruiker op een oude schil terwijl `/api/health` netjes de nieuwe SHA meldt.

**Files:**
- Create: `app/components/PwaUpdatePrompt.vue`
- Modify: `app/app.vue`
- Modify: `i18n/locales/en.json`, `nl.json`, `fr.json`
- Create: `e2e/pwa/update.spec.ts`

**Interfaces:**
- Consumes: `registerType: 'prompt'` uit Taak 1.
- Produces: niets waar latere taken op steunen.

- [ ] **Step 1: Voeg de vertalingen toe**

In `en.json`, naast het `offline`-blok:

```json
  "pwa": {
    "updateAvailable": "A new version is available.",
    "reload": "Reload"
  },
```

In `nl.json`:

```json
  "pwa": {
    "updateAvailable": "Er is een nieuwe versie beschikbaar.",
    "reload": "Herladen"
  },
```

In `fr.json`:

```json
  "pwa": {
    "updateAvailable": "Une nouvelle version est disponible.",
    "reload": "Recharger"
  },
```

- [ ] **Step 2: Schrijf de test**

Maak `e2e/pwa/update.spec.ts`:

```ts
import { test, expect } from '@playwright/test'
import en from '../../i18n/locales/en.json' with { type: 'json' }

// Het positieve geval — er is werkelijk een nieuwe versie — vraagt twee
// builds en is daarom een handmatige controle (zie Step 6). Wat hier wél te
// automatiseren valt is het negatieve geval, en dat is niet niks: een
// component die altijd rendert zou elke gebruiker bij elk bezoek een
// updatemelding tonen voor een update die er niet is.
test('er staat geen updatemelding bij een verse installatie', async ({ page }) => {
  await page.goto('/')
  await page.evaluate(() => navigator.serviceWorker.ready)

  await expect(page.getByText(en.pwa.updateAvailable)).toHaveCount(0)
})
```

- [ ] **Step 3: Draai de test**

Run: `npm run test:e2e:pwa`
Expected: PASS (er is nog geen component, dus er staat ook niets).

Dit is een test die vooruitloopt op de implementatie in plaats van erop te wachten. Dat is hier juist het punt: hij bewaakt dat de component die je zo maakt zich stil houdt.

- [ ] **Step 4: Maak de component**

Maak `app/components/PwaUpdatePrompt.vue`:

```vue
<script setup lang="ts">
// registerType: 'prompt' in nuxt.config.ts zorgt dat een nieuwe service
// worker blijft wachten tot iemand hem binnenlaat. needRefresh wordt waar
// zodra die nieuwe versie klaarstaat.
//
// Waarom niet 'autoUpdate': elke merge naar main deployt, dus updates komen
// vaak. Automatisch herladen zou dat kunnen doen terwijl iemand een
// huishoudnaam of een uitnodiging staat in te vullen.
const { needRefresh, updateServiceWorker } = useRegisterSW()
const { t } = useI18n()
</script>

<template>
  <div
    v-if="needRefresh"
    class="fixed inset-x-0 bottom-0 z-50 border-t border-muted bg-default p-4"
  >
    <UContainer class="flex items-center justify-between gap-4">
      <p>{{ t('pwa.updateAvailable') }}</p>
      <UButton size="sm" @click="updateServiceWorker(true)">
        {{ t('pwa.reload') }}
      </UButton>
    </UContainer>
  </div>
</template>
```

Heet de composable anders, kijk dan in `node_modules/@vite-pwa/nuxt/dist/` welke composables de module automatisch importeert, en gebruik die naam.

- [ ] **Step 5: Neem hem op in app.vue**

Plaats `<PwaUpdatePrompt />` in `app/app.vue`, binnen `<UApp>` en na `<NuxtPage />`.

- [ ] **Step 6: Controleer het positieve geval met de hand**

1. `npm run build && npx wrangler --cwd .output dev --port 3001`
2. Open `http://localhost:3001/` en wacht tot de service worker actief is (DevTools → Application → Service Workers).
3. Laat de server draaien. Wijzig in een tweede terminal iets zichtbaars, bijvoorbeeld de tagline in `i18n/locales/en.json`, en draai `npm run build` opnieuw.
4. Herstart de wrangler-server zodat hij de nieuwe build serveert.
5. Ververs het tabblad.

Expected: de melding verschijnt onderaan. Na een klik op Herladen toont de pagina de gewijzigde tagline.

Draai de wijziging in de tagline terug. Rapporteer wat je zag.

- [ ] **Step 7: Draai de testsuite**

Run: `npm run test:e2e:pwa && npm run lint && npm run typecheck`
Expected: alles schoon.

- [ ] **Step 8: Commit**

```bash
git add app/components/PwaUpdatePrompt.vue app/app.vue i18n/locales/ e2e/pwa/update.spec.ts
git commit -m "feat: een zichtbare melding als er een nieuwe versie klaarstaat

Elke merge naar main deployt. Zonder dit blijft een gebruiker op een oude
schil terwijl /api/health keurig de nieuwe SHA meldt.

Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>"
```

---

### Task 7: De taalprefix bij de auth-bounce

De guard van `@nuxtjs/supabase` stuurt altijd naar het kale `/login`; `redirectOptions.login` is één vaste string. Tot nu toe was dat een schoonheidsfout op een pad dat je zelden raakt. Met `start_url: '/nl/voorraad'` wordt het het eerste scherm dat een uitgelogde gebruiker ziet nadat hij op het icoon tikt.

**Files:**
- Modify: `app/pages/login.vue`
- Create: `e2e/login-taal.spec.ts` (of een test toevoegen aan `e2e/login.spec.ts`)

**Interfaces:**
- Consumes: `localeCodes` uit `routes.config.ts`.
- Produces: niets waar latere taken op steunen.

- [ ] **Step 1: Schrijf de faalende test**

Voeg toe aan `e2e/login.spec.ts` (deze test draait tegen de dev-server, niet tegen de PWA-configuratie):

```ts
import { localeCodes, routePath } from '../routes.config'

// De auth-guard van @nuxtjs/supabase stuurt altijd naar het kale /login,
// zonder taalprefix. Wie de app in het Nederlands gebruikt en uitgelogd op
// zijn startscherm-icoon tikt, belandt daardoor op een Engelse inlogpagina.
test('de inlogpagina volgt je taalkeuze als je zonder prefix binnenkomt', async ({ page, context }) => {
  await context.addCookies([
    { name: 'stash_locale', value: 'nl', url: 'http://localhost:3000' },
  ])

  await page.goto(routePath('login', 'en'))

  await expect(page).toHaveURL(new RegExp(`${routePath('login', 'nl')}$`))
})

// De keerzijde: wie het Engels gekozen heeft moet op de Engelse pagina
// blijven staan. Zonder deze test zou een doorstuur die áltijd naar het
// Nederlands gaat de vorige test halen.
test('de inlogpagina stuurt niet door als je taalkeuze al klopt', async ({ page, context }) => {
  await context.addCookies([
    { name: 'stash_locale', value: 'en', url: 'http://localhost:3000' },
  ])

  await page.goto(routePath('login', 'en'))

  await expect(page).toHaveURL(new RegExp(`${routePath('login', 'en')}$`))
})
```

Controleer of `localeCodes` in dit bestand werkelijk gebruikt wordt; is dat niet zo, laat de import weg zodat lint niet klaagt.

- [ ] **Step 2: Draai de test en controleer dat hij faalt**

Run: `npm run test:e2e -- login`
Expected: de eerste test FAILt (de URL blijft `/login`), de tweede slaagt al.

- [ ] **Step 3: Stuur door vanuit login.vue**

Voeg bovenaan het `<script setup>`-blok van `app/pages/login.vue` toe:

```ts
import { localeCodes, type LocaleCode } from '~~/routes.config'

const { locale } = useI18n()
const localePath = useLocalePath()

// De auth-guard van @nuxtjs/supabase stuurt altijd naar het kale /login:
// redirectOptions.login is één vaste string en kan de taal niet weten. Wie de
// app in het Nederlands gebruikt en uitgelogd op zijn startscherm-icoon tikt,
// belandt dus op een Engelse inlogpagina.
//
// De taalkeuze staat al in de cookie die detectBrowserLanguage zet
// (cookieKey 'stash_locale' in nuxt.config.ts). Die volgen we hier alsnog.
//
// Niet gekozen: de redirect van @nuxtjs/supabase helemaal vervangen door
// eigen auth-middleware. Dat lost hetzelfde op maar raakt elke afgeschermde
// route in de app.
const gekozenTaal = useCookie<string | null>('stash_locale')

if (
  gekozenTaal.value
  && gekozenTaal.value !== locale.value
  && localeCodes.includes(gekozenTaal.value as LocaleCode)
) {
  await navigateTo(localePath('login', gekozenTaal.value as LocaleCode))
}
```

`~~` is in Nuxt 4 het alias voor de projectwortel (`~` wijst naar `app/`), dus `~~/routes.config` klopt. Werkt de import toch niet, gebruik dan het relatieve pad `'../../routes.config'` zoals `e2e/helpers.ts` doet.

Controleer of het bestaande `<script setup>`-blok al `useI18n()` aanroept; zo ja, breid de destructurering uit in plaats van een tweede aanroep toe te voegen.

`await navigateTo(...)` op het hoogste niveau van `<script setup>` maakt de component async. Dat mag in Nuxt, maar controleer of `login.vue` al top-level `await` gebruikt; zo niet, let er dan op dat de pagina nog steeds rendert voor wie niet doorgestuurd wordt.

- [ ] **Step 4: Draai de tests en controleer dat ze slagen**

Run: `npm run test:e2e -- login`
Expected: PASS, beide tests.

- [ ] **Step 5: Draai de volledige e2e-suite**

Run: `npm run test:e2e`
Expected: alle bestaande tests blijven groen. Let vooral op `login.spec.ts` en `locales.spec.ts` — de doorstuur mag de bestaande inlogflow niet breken.

- [ ] **Step 6: Falsificatie**

Zet het `if`-blok tijdelijk in commentaar en draai `npm run test:e2e -- login`.
Expected: de eerste test wordt rood, de tweede blijft groen.

Zet het terug en rapporteer.

- [ ] **Step 7: Commit**

```bash
git add app/pages/login.vue e2e/login.spec.ts
git commit -m "fix: de inlogpagina volgt je taalkeuze ook zonder prefix

Vierde en laatste geval van taalverlies uit open-bevindingen.md. Met een
geinstalleerde app is dit het eerste scherm dat een uitgelogde gebruiker
ziet, in een taal die hij niet koos.

Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>"
```

---

### Task 8: README en open bevindingen

**Files:**
- Modify: `README.md`
- Modify: `docs/superpowers/open-bevindingen.md`

**Interfaces:**
- Consumes: alles uit de voorgaande taken.
- Produces: niets.

- [ ] **Step 1: Beschrijf de PWA in README.md**

Voeg een sectie toe, in het Engels zoals de rest van dat bestand. Beschrijf:

- dat de app installeerbaar is, en dat het icoon de voorraad in de taal van de installatie opent
- dat `npm run icons` de icons opnieuw genereert uit `public/icon.svg`
- dat `npm run test:e2e:pwa` tegen een gebouwde app draait op poort 3001, en waarom dat niet tegen de dev-server kan
- dat de offline-schil werkt maar de data nog niet, met een verwijzing naar de spec

- [ ] **Step 2: Werk de open bevindingen bij**

Verwijder de bevinding over de auth-guard die de taalprefix negeert; die is opgelost in Taak 7.

Voeg toe onder "Kleine punten":

- **Geen eigen installatieknop.** Browsers bieden installatie zelf aan, en op iOS bestaat `beforeinstallprompt` niet — daar loopt het via het deelmenu. Een eigen knop zou op ongeveer de helft van de toestellen een uitleg moeten tonen in plaats van iets te doen. Bewust uitgesteld; als het alsnog moet, is het een apart stukje ontwerp met een iOS-tekst erbij.
- **Offline data.** De schil opent zonder netwerk, de voorraad nog niet. Zodra `stock_item` bestaat, is dit het moment om de spec-belofte uit §9 waar te maken.

Voeg toe onder "Valkuilen bij lokaal ontwikkelen":

- **`wrangler dev` laat `workerd` achter.** Het kindproces overleeft een `taskkill` op de ouder en houdt de poort bezet, waarna de volgende testrun op een bezette poort stuit of — erger — een oude build blijft serveren. Stop eerst het `node`-proces met `wrangler` in zijn commandline, daarna `workerd`.

- [ ] **Step 3: Draai alles nog één keer**

Run: `npm run lint && npm run typecheck && npm test && npm run test:e2e && npm run test:e2e:pwa`
Expected: alles groen. Rapporteer de aantallen per suite.

- [ ] **Step 4: Commit**

```bash
git add README.md docs/superpowers/open-bevindingen.md
git commit -m "docs: de PWA beschrijven, en de bevindingen bijwerken

Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>"
```
